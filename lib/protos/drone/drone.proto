syntax = "proto3";

message Drone {
  RemoteID remoteID = 1;

  message RemoteID {
    Connection connection = 1;
    Location location = 2;
  }

  message Connection {
    string macAddress = 1;
    int32 rssi = 2;
  }

  message Location {
    double latitude = 1;
    double longitude = 2;
  }
}